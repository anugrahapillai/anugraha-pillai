# Anughara

Next.js App Router foundation for the Anugraha Pillai portfolio and private CMS.

## Start locally

1. Copy `.env.example` to `.env.local` and fill in development values.
2. Run `npm install`.
3. Run `npm run dev`.

The application uses JavaScript, Tailwind CSS, Firebase, Zod, Vitest and Playwright.
